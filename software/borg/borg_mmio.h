#pragma once

// Auto-generated by MmioGenerator.scala — do not edit manually

// --- MmioMap Constants (Auto-Extracted) ---
#define BORG_BASE 0x08000600
#define BORG_CONTROL_OFFSET 264
#define BORG_CTL_PC_BITS 5
#define BORG_CTL_PC_LSB 5
#define BORG_CTL_PC_MASK 31
#define BORG_CTL_PC_MSB 9
#define BORG_CTL_PC_SHIFT 5
#define BORG_CTL_RESET 2
#define BORG_CTL_START 1
#define BORG_IMEM_END 256
#define BORG_IMEM_OFFSET 128
#define BORG_IMEM_SLOTS 32
#define BORG_ITER_BBOX_OFFSET 256
#define BORG_ITER_BBOX_X0_SHIFT 0
#define BORG_ITER_BBOX_X1_SHIFT 12
#define BORG_ITER_BBOX_Y0_SHIFT 6
#define BORG_ITER_BBOX_Y1_SHIFT 18
#define BORG_ITER_COORD_BITS 6
#define BORG_ITER_COORD_MASK 63
#define BORG_ITER_OFFSET 260
#define BORG_ITER_VALID_SHIFT 12
#define BORG_ITER_X_SHIFT 0
#define BORG_ITER_Y_SHIFT 6
#define BORG_NUM_REGS 32
#define BORG_REG_OFFSET 0
#define BORG_STS_IDLE 2
#define BUS_IDLE 3
#define CLOCK_MHZ 4
#define DONE_MARKER 0x0000DEAD
#define FPGA_CLOCK_HZ 4000000
#define GPIO_BASE 0x08000200
#define GPIO_FUNC_SEL_BIT 5
#define GPIO_FUNC_SEL_IDX_HI 4
#define GPIO_FUNC_SEL_IDX_LO 2
#define GPIO_IN_OFFSET 4
#define GPIO_OUT_OFFSET 0
#define PERI_DEBUG 12
#define PERI_DEBUG_UART 6
#define PERI_DEBUG_UART_BAUD 8
#define PERI_DEBUG_UART_STATUS 7
#define PERI_GPIO_OUT_SEL 3
#define PERI_ID 2
#define PERI_NONE 0
#define PERI_TIME_LIMIT 11
#define PERI_USER 15
#define PSRAM_BASE 0x01001000
#define PSRAM_OUT_OFFSET 128
#define PSRAM_SPI_BASE 0x00001000
#define SOC_PERI_DEBUG_UART_OFFSET 24
#define SOC_PERI_GPIO_OUT_SEL_OFFSET 12
#define SOC_PERI_ID_OFFSET 8
#define SOC_PERI_TIME_LIMIT_OFFSET 44
#define SPIRB_INSTR_BYTES 4
#define STARTUP_DELAY_CYCLES 10000
#define TEX_PSRAM_OFFSET 4200
#define UART_BASE 0x08000400
#define UART_BAUD_DEFAULT 34
#define UART_BAUD_OFFSET 8
#define UART_STATUS_OFFSET 4
#define UART_TX_OFFSET 0
#define USER_BASE 0x08000000
#define USER_PERI_BORG 3
#define USER_PERI_GPIO 1
#define USER_PERI_SEL_HI 10
#define USER_PERI_SEL_LO 9
#define USER_PERI_STRIDE 512
#define USER_PERI_UART 2
#define USER_SUB_ADDR_HI 8
#define USER_SUB_ADDR_LO 0

// --- Borg instruction encoding (32-bit RISC-V R-type / R4-type) ---
#define BORG_INSTR_FADD(rd, rs1, rs2)       (0x00000000UL | ((rs2) << 20) | ((rs1) << 15) | ((rd) << 7))
#define BORG_INSTR_FMUL(rd, rs1, rs2)       (0x08000000UL | ((rs2) << 20) | ((rs1) << 15) | ((rd) << 7))
#define BORG_INSTR_FMADD(rd, rs1, rs2, rs3) (0x00000004UL | ((rs3) << 27) | ((rs2) << 20) | ((rs1) << 15) | ((rd) << 7))
#define BORG_INSTR_FNEG(rd, rs1)            (0x0C000000UL | ((rs1) << 15) | ((rd) << 7))
#define BORG_INSTR_FSTEP(rd, rs1)           (0x10000000UL | ((rs1) << 15) | ((rd) << 7))
#define BORG_INSTR_FRCP(rd, rs1)            (0x14000000UL | ((rs1) << 15) | ((rd) << 7))
#define BORG_INSTR_HALT                     0x00000000UL

// --- C Macros for Address Casting ---
#define UART_TX           (*(volatile uint32_t *)(UART_BASE + UART_TX_OFFSET))
#define UART_STATUS       (*(volatile uint32_t *)(UART_BASE + UART_STATUS_OFFSET))
#define UART_BAUD         (*(volatile uint32_t *)(UART_BASE + UART_BAUD_OFFSET))
#define BORG_REG(n)       (*(volatile uint32_t *)(BORG_BASE + BORG_REG_OFFSET + (n) * 4))
#define BORG_IMEM(n)      (*(volatile uint32_t *)(BORG_BASE + BORG_IMEM_OFFSET + (n) * 4))
#define BORG_CONTROL      (*(volatile uint32_t *)(BORG_BASE + BORG_CONTROL_OFFSET))
#define BORG_STATUS       (*(volatile uint32_t *)(BORG_BASE + BORG_CONTROL_OFFSET))
#define BORG_CTL_PC(pc)           (((pc) & BORG_CTL_PC_MASK) << BORG_CTL_PC_SHIFT)
#define BORG_ITER_BBOX    (*(volatile uint32_t *)(BORG_BASE + BORG_ITER_BBOX_OFFSET))
#define BORG_ITER         (*(volatile uint32_t *)(BORG_BASE + BORG_ITER_OFFSET))
#define BORG_ITER_PACK_BBOX(x0,y0,x1,y1) (((y1)<<BORG_ITER_BBOX_Y1_SHIFT)|((x1)<<BORG_ITER_BBOX_X1_SHIFT)|((y0)<<BORG_ITER_BBOX_Y0_SHIFT)|((x0)<<BORG_ITER_BBOX_X0_SHIFT))
#define BORG_ITER_X(v)            (((v) >> BORG_ITER_X_SHIFT) & BORG_ITER_COORD_MASK)
#define BORG_ITER_Y(v)            (((v) >> BORG_ITER_Y_SHIFT) & BORG_ITER_COORD_MASK)
#define BORG_ITER_VALID(v)        (((v) >> BORG_ITER_VALID_SHIFT) & 1)
#define PSRAM_IN(n)       (*(volatile uint32_t *)(PSRAM_BASE + (n) * 4))
#define PSRAM_OUT(n)      (*(volatile uint32_t *)(PSRAM_BASE + PSRAM_OUT_OFFSET + (n) * 4))

// --- Convenience ---
#define STARTUP_DELAY() do { \
    for (volatile int i = 0; i < STARTUP_DELAY_CYCLES; i++) ; \
  } while (0)
